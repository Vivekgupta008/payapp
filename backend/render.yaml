services:
  - type: web
    name: offlinepay-api
    runtime: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ED25519_PRIVATE_KEY_B64
        sync: false          # paste the value from first-run logs
      - key: DATABASE_URL
        fromDatabase:
          name: offlinepay-db
          property: connectionString

databases:
  - name: offlinepay-db
    plan: free
    region: singapore
